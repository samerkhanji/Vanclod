/* Everything you are likely to edit lives here. */
window.VANCLOD = {

  // Where booking enquiries go. REPLACE with the real booking address.
  bookingEmail: "bookings@vanclod.com",

  // Optional. Paste a form endpoint (Formspree, Basin, Web3Forms, your own server)
  // and the booking form will send silently instead of opening the visitor's email app.
  // Leave empty to keep the email-draft behaviour.
  formEndpoint: "",

  // Upcoming shows. Past dates hide themselves; an empty list shows a "follow for dates" panel.
  // { date: "2026-11-14", event: "Event name", venue: "Venue", city: "Beirut", link: "https://…" }
  shows: [],

  // SoundCloud numeric user id, used by the embedded player.
  soundcloudUserId: "87327596"
};
